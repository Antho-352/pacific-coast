<div class="bg-slate-800 rounded-xl p-6 shadow-xl border border-slate-700">
  <div class="flex justify-between mb-4">
    <h3 class="text-xl font-bold text-white">🧠 Quiz Moto</h3>
    <span id="score" class="text-red-500 font-bold">0/5</span>
  </div>
  <div id="quiz-box">
    <h4 id="q-text" class="text-lg text-white mb-4">Chargement...</h4>
    <div id="opts" class="space-y-2"></div>
    <div id="feed" class="mt-4 hidden p-2 rounded text-sm"></div>
    <button id="next" class="mt-4 w-full bg-red-600 text-white py-2 rounded hidden">Suivant</button>
  </div>
</div>

<script>
  const qs = [
    {q:"Année PC800?", o:["1985","1989","1995"], a:1, e:"Lancée en 1989."},
    {q:"Volume coffre?", o:["30L","48L","60L"], a:1, e:"48 Litres."},
    {q:"Transmission?", o:["Chaîne","Cardan","Courroie"], a:1, e:"Cardan."},
    {q:"Cylindrée?", o:["500cc","800cc","1000cc"], a:1, e:"V-Twin 800cc."},
    {q:"Origine design?", o:["Moto GP","Auto Honda","Yamaha"]    {q:"Origine design?", o:["Moto GP","Auto Honda","Yamah;
  const els = {t:document.getElementById('q-text'), o:document.getElementById('opts'), f:document.getElementById('feed'), n:document.getElementById('next'), s:document.getElementById('score')};
  
  function load() {
    const q = qs[cur];
    if(!els.t) return;
    els.t.innerText = q.q; els.o.innerHTML=''; els.f.classList.add('hidden'); els.n.classList.add('hidden');
    q.o.forEach((opt,i) => {
      const b = document.createElement('button');
      b.className="w-full text-left p-2 rounded bg-slate-700 text-white hover:bg-slate-600";
      b.innerText=opt;
      b.onclick=()=>check(i,b);
      els.o.appendChild(b);
    });
  }
  function check(i,b) {
    const q=qs[cur];
    if(i===q.a) { sc++; b.classList.add('bg-green-600'); els.f.innerText="✅ "+q.e; els.f.className="mt-4 block p-2 bg-green-900 text-green-200"; }
    else { b.classList.add('bg-red-600'); els.f.innerText="❌ "+q.e; els.f.className="mt-4 block p-2 bg-red-900 text-red-200"; }
    els.s.innerText=sc+"/5"; els.n.classList.remove('hidden');
  }
  if(els.n) els.n.onclick=()=>{ cur++; if(cur<qs.length) load(); else els.t.innerText="Fini! Score: "+sc+"/5"; };
  load();
</script>
