---
const currentYear = new Date().getFullYear();

const footerLinks = {
  navigation: [
    { label: "Accueil", href: "/" },
    { label: "L'Histoire", href: "/histoire" },
    { label: "Fiche Technique", href: "/specs" },
    { label: "Galerie", href: "/#galerie" },
  ],
  legal: [
    { label: "Mentions Légales", href: "/mentions-legales" },
    { label: "Plan du site", href: "/plan-du-site" },
  ],
};
---

<footer class="bg-metal-900 border-t border-metal-800">
  <div class="container mx-auto px-4 py-12">
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      <!-- Logo & Description -->
      <div>
        <a href="/" class="text-xl font-bold text-white tracking-tight">
          PC800<span class="text-red-500">.fr</span>
        </a>
        <p class="text-gray-400 text-sm mt-3 leading-relaxed">
          Le site de référence pour les passionnés de la Honda Pacific Coast PC800. Partage, entraide et passion du touring.
        </p>
      </div>

      <!-- Navigation -->
      <div>
        <h3 class="text-white font-semibold text-sm uppercase tracking-wider mb-4">Navigation</h3>
        <ul class="space-y-2">
          {footerLinks.navigation.map((link) => (
            <li>
              <a href={link.href} class="text-gray-400 hover:text-white text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <!-- Legal -->
      <div>
        <h3 class="text-white font-semibold text-sm uppercase tracking-wider mb-4">Informations</h3>
        <ul class="space-y-2">
          {footerLinks.legal.map((link) => (
            <li>
              <a href={link.href} class="text-gray-400 hover:text-white text-sm transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <!-- Disclaimer -->
    <div class="mt-10 pt-8 border-t border-metal-800">
      <div class="bg-metal-800/50 rounded-lg p-4 mb-6">
        <p class="text-gray-500 text-xs text-center leading-relaxed">
          <strong class="text-gray-400">Disclaimer :</strong> Ce site est un projet communautaire indépendant.
          <strong class="text-gray-400"> Non affilié à Honda Motor Co., Ltd.</strong>
          Honda et Pacific Coast sont des marques déposées de Honda Motor Co., Ltd.
        </p>
      </div>

      <div class="flex flex-col md:flex-row items-center justify-between text-gray-500 text-sm">
        <p>&copy; {currentYear} PC800.fr. Tous droits réservés.</p>
        <p class="mt-2 md:mt-0">Site créé avec passion par des motards.</p>
      </div>
    </div>
  </div>
</footer>

<style>
  .container {
    width: 100%;
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
  }
  
  .bg-metal-900 { background-color: #1C1917; }
  .bg-metal-800 { background-color: #292524; }
  .border-metal-800 { border-color: #292524; }
</style>